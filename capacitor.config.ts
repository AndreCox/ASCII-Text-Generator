//update app name and appID here
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rockettemplate.app',
  appName: 'Rocket Template',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
